{% extends 'base.html.twig' %}
 
{% block title %}New Country{% endblock %}
 
{% block body %}
    <div class="container">
        <h2 class="text-center mt-5 mb-3">Create Country</h2>
        <div class="card">
            <div class="card-header">
                <a class="btn btn-outline-info float-right" href="{{ path('country_index') }}"> 
                    View All countries
                </a>
            </div>
            <div class="card-body">
                 {{ include('country/_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}